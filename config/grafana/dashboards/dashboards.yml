apiVersion: 1

providers:
  - name: "QuantStream"
    orgId: 1
    folder: "QuantStream"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
